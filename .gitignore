node_modules
.next
out
.env*
.DS_Store
.vercel

# Working material and private notes. Local only, never committed.
workspace/

# Agent rules. Lives at the repo root so tools auto-load it, but is not pushed.
/AGENTS.md
